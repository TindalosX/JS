const letras = ['a', 'b', 'c', 'd', 'e'];

// for
for (i = 0; i < letras.length; i++) {
	console.log(letras[i]);
}
console.log();

//while

let cont = 0;

while ( cont < letras.length) {
	console.log(letras[cont]);
	cont++;
}
